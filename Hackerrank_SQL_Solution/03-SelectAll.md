# Select All

## Problem Statement:
Query all columns (attributes) for every row in the CITY table.

The CITY table is described as follows:

![](./Images/City.PNG)

## Solution:
```SQL
SELECT *
FROM CITY
```